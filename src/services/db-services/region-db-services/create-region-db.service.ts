//TODO fill in function

import prismaClient from '../../../lib/prisma/client.prisma';

const createRegionDB = async () => {
  try {
  } catch (error) {
    throw error;
  }
};

export default createRegionDB;
