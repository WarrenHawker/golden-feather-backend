//TODO fill in function

import prismaClient from '../../../lib/prisma/client.prisma';

const updateRegionDB = async () => {
  try {
  } catch (error) {
    throw error;
  }
};

export default updateRegionDB;
