//TODO fill in function

import prismaClient from '../../../lib/prisma/client.prisma';

const deleteLanguageDB = async () => {
  try {
  } catch (error) {
    throw error;
  }
};

export default deleteLanguageDB;
