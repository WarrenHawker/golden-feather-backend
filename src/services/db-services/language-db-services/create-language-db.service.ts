//TODO fill in function

import prismaClient from '../../../lib/prisma/client.prisma';

const createLanguageDB = async () => {
  try {
  } catch (error) {
    throw error;
  }
};

export default createLanguageDB;
