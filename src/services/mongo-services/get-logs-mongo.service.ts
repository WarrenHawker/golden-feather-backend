//TODO fill in function

const getLogsMongo = async (
  page?: number,
  limit?: number,
  levels?: string[],
  months?: string[],
  startDate?: Date,
  endDate?: Date
) => {
  return [];
};

export default getLogsMongo;
